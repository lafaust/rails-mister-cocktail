<h1>Cocktails (cards)</h1>
  <ul>
      <% @cocktails.each do |cocktail| %>
       <li> <%= cocktail.name %> <%= link_to "see details", cocktail_path(cocktail.id)  %></li>
      <% end %>
  </ul>
<%= link_to "Add a cocktail", new_cocktail_path, class: "btn btn-primary"  %>

